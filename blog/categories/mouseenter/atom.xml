<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

  <title><![CDATA[Category: mouseenter | Liu.Shatle]]></title>
  <link href="http://shatle.github.com/blog/categories/mouseenter/atom.xml" rel="self"/>
  <link href="http://shatle.github.com/"/>
  <updated>2014-03-04T09:29:57+08:00</updated>
  <id>http://shatle.github.com/</id>
  <author>
    <name><![CDATA[liu.shatle &hearts; gmail.com]]></name>
    
  </author>
  <generator uri="http://octopress.org/">Octopress</generator>

  
  <entry>
    <title type="html"><![CDATA[show and hide element in angularjs]]></title>
    <link href="http://shatle.github.com/blog/2013/12/30/show-and-hide-element-in-angularjs/"/>
    <updated>2013-12-30T11:35:00+08:00</updated>
    <id>http://shatle.github.com/blog/2013/12/30/show-and-hide-element-in-angularjs</id>
    <content type="html"><![CDATA[<p>由于是开始接触angularjs，所以很多效果都是用jquery/js的思维去做的，但是angularjs是不提倡直接操作dom元素的。这一直很让我纠结，慢慢去修正吧。</p>

<p>相信，显示与隐藏是十分常见的功能／效果。刚开始，我是用jquery去实现，</p>

<p>```javascript
$('.ele')
  .mouseenter(function(){</p>

<pre><code>$(this).find('.text').show();
</code></pre>

<p>  })
  .mouseleave(function(){</p>

<pre><code>$(this).find('.text').hide();
</code></pre>

<p>  });
```</p>

<p>但是这就直接操作dom了，怎么避免这些不好的操作呢，就要使用angular提供的或者自己编写的directives。</p>

<p>从<a href="http://docs.angularjs.org/api/ng.directive">官网</a>页面中可以找到相关的directives：<code>ngHide</code>、<code>ngShow</code>、<code>ngMouseenter</code>、<code>ngMouseleave</code>。</p>

<p>```haml
%a( ng-repeat="item in items"</p>

<pre><code>ng-mouseenter="hover(item)"
ng-mouseleave="hover(item)"
</code></pre>

<p>  )
  %img(ng-src="...")
  .text(ng-show="item.show")</p>

<pre><code>%span item.name
</code></pre>

<p>```</p>

<p>```javascript
app.controller('indexCtrl', ['$scope', function($scope){
  $scope.items = [</p>

<pre><code>{
  name: '..'
}
,{
  name: '....'
}
</code></pre>

<p>  ];</p>

<p>  $scope.hover = function(item){</p>

<pre><code>item.show = !item.show;
</code></pre>

<p>  };</p>

<p>}]);
```</p>

<p>效果是，默认的文字名称是隐藏的，因为开始时<code>item.show</code>是没有设置的，为false。
当鼠标移进移出时，会触发<code>hover</code>方法 ，修改相应<code>item.show</code>的值。</p>
]]></content>
  </entry>
  
</feed>
